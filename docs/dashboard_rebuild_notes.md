# Dashboard Rebuild Notes

The SQL query was updated and a new CSV dataset was exported for Tableau.

The dashboard is being rebuilt using the updated dataset.
The goal of this rebuild is to improve the accuracy of revenue metrics and prepare the final version of the Tableau dashboard.

Current progress:

* Updated SQL query;
* Exported a new CSV dataset;
* Created a new Tableau dashboard;
* Started rebuilding visualizations with the refreshed data.

Next steps:

* Recreate key dashboard charts;
* Validate metric values in Tableau;
* Add updated dashboard screenshots to the README;
* Publish the final Tableau Public dashboard link.


After refreshing the dataset, the Tableau dashboard was rebuilt from the beginning to make sure all revenue metrics are based on the updated calculation logic.

Completed rebuild progress:

Updated revenue metrics SQL query;
Exported a refreshed CSV dataset;
Created a new Tableau dashboard version;
Rebuilt previous dashboard charts using the updated dataset;
Added the Back from Churn MRR visualization.

Remaining work:

Build the Revenue Change Factors chart;
Validate final dashboard values;
Add final dashboard screenshots to README;
Publish the Tableau Public dashboard link.
